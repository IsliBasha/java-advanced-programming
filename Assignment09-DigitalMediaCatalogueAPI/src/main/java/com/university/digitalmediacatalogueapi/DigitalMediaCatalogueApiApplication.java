package com.university.digitalmediacatalogueapi;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Entry point for the Digital Media Catalogue REST API. */
@SpringBootApplication
public class DigitalMediaCatalogueApiApplication {
    public static void main(String[] args) {
        SpringApplication.run(DigitalMediaCatalogueApiApplication.class, args);
    }
}
